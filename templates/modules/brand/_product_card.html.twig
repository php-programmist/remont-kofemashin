<div class="cartochkabrenda">
    <div class="container">
        <div class="cartochkabrenda-container">
            <div class="row">
                <div class="col-lg-5  cartochkabrenda-img">
                    <img src="{{ asset('img/brand_image/'~brand.image) }}" alt="{{ brand.name }}">
                </div>
                <div class="col-lg-7  cartochkabrenda-text">
                    <div class="zag2">Ремонт кофемашины {{ brand.name }}{{ model.name is defined ?' '~model.name }}</div>
                    <p>Наш ремонтный центр предлагает владельцам кофемашин {{ brand.name }}{{ model.name is defined ?' '~model.name }}, ассортимент сервисных услуг, выполнением которых будут заниматься дипломированные инженеры с большим стажем работы. Посетив нашу сервисную мастерскую, вы сможете убедиться в том, что она оборудована по последнему слову и техники, а нашем личном складе имеется запас деталей ко всем моделям Bosch.</p>
                    <ul>
                        <li>✅ Бесплатная доставка до сервиса.</li>
                        <li>✅ Гарантия на ремонт кофемашин {{ brand.name }}{{ model.name is defined ?' '~model.name }} от 6 месяцев.</li>
                        <li>✅ Ремонтируем только то, что действительно сломалось!</li>
                    </ul>
                    <div class="price">Ремонт кофемашины {{ brand.name }}{{ model.name is defined ?' '~model.name }} от 450₽</div>
                    <p class="minutp">Получите предложение уже через 7 минут</p>
                    <button class="btn btn-vizovbespldiagn">Бесплатная диагностика</button>
                    <button class="btn btn-vibratmodel">Выбрать модель</button>
                </div>
                <div class="spisokmodelei">
                    <div class="row">
                        {% for model in brand.models %}
                            <a href="{{ path('model_or_service',{'brand':brand.alias,'model_or_service':model.alias}) }}" class="col-xl-3 col-lg-3 col-sm-6">{{ brand.name }} {{ model.name }}</a>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>