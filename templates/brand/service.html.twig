{% extends 'page_with_callback_form.html.twig' %}

    {% block title %}{{ service.seoName }} {{ service.isService ? 'по низкой цене в Москве - «Ремонт Кофемашин»':'- «Ремонт Кофемашин»' }}{% endblock %}

    {% block description %}{{ service.seoName }}.{% endblock %}

    {% block description_price %}{{ service.price }}{% endblock %}

    {% block h1 %}
        {% if(service.isService) %}
            {{ service.seoName }} в Москве
        {% else %}
            {{ service.seoName }}
        {% endif %}
    {% endblock %}
    {% block description_repair %}{{ service.isService == 0 ? 'ремонта' }}{% endblock %}

{% block content %}
    {{ include('modules/_service_content.html.twig') }}
{% endblock %}

